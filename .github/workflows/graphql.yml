name: Graphql

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  args-query:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        query: samples/jira.query.yaml
        owner: helaili
        name: github-graphql-action
        


  
